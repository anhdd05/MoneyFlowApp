<Window x:Class="MoneyFlowApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoneyFlowApp"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Đăng nhập - MoneyFlow" Height="380" Width="400"
        ResizeMode="NoResize">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MoneyFlow" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,24" Foreground="DarkGreen"/>

        <TextBlock Grid.Row="1" Text="Email:" VerticalAlignment="Center" Margin="0,0,0,4"/>
        <TextBox Grid.Row="2" x:Name="TxtEmail" Height="32" Margin="0,0,0,16" 
                 Padding="8,4" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="3" Text="Mật khẩu:" VerticalAlignment="Center" Margin="0,0,0,4"/>
        <PasswordBox Grid.Row="4" x:Name="TxtPassword" Height="32" Margin="0,0,0,20"
                     Padding="8,4" VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="5" x:Name="TxtError" Foreground="Red" TextWrapping="Wrap" 
                   Margin="0,0,0,12" Visibility="Collapsed"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button x:Name="BtnLogin" Content="Đăng nhập" Width="100" Height="36" Click="BtnLogin_Click" 
                    Margin="0,0,8,0" Background="DarkGreen" Foreground="White"/>
            <Button x:Name="BtnCancel" Content="Thoát" Width="80" Height="36" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
