<UserControl x:Class="MoneyFlowApp.Admin.AdminDashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="700">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="Tổng quan Hệ thống" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="#E8F5E9" CornerRadius="4" Padding="16" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Tổng User" FontSize="12" Foreground="Gray"/>
                        <TextBlock x:Name="TxtTotalUsers" FontSize="24" FontWeight="Bold" Foreground="DarkGreen"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="#E3F2FD" CornerRadius="4" Padding="16" Margin="8,0">
                    <StackPanel>
                        <TextBlock Text="User Active" FontSize="12" Foreground="Gray"/>
                        <TextBlock x:Name="TxtActiveUsers" FontSize="24" FontWeight="Bold" Foreground="#1976D2"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="#FFF3E0" CornerRadius="4" Padding="16" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Tổng Giao dịch" FontSize="12" Foreground="Gray"/>
                        <TextBlock x:Name="TxtTotalTransactions" FontSize="24" FontWeight="Bold" Foreground="#E65100"/>
                    </StackPanel>
                </Border>
            </Grid>

            <TextBlock Text="Đăng ký mới (30 ngày qua)" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="16" MinHeight="180">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="ChartBars">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="24" Margin="2" VerticalAlignment="Bottom">
                                    <Border Background="#4CAF50" Height="{Binding Height}" MinHeight="4" CornerRadius="2" VerticalAlignment="Bottom"/>
                                    <TextBlock Text="{Binding Label}" FontSize="8" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
