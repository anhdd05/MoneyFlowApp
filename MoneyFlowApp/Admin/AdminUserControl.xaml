<UserControl x:Class="MoneyFlowApp.Admin.AdminUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="700">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="TxtSearch" Width="200" Height="28" Padding="8,4" VerticalContentAlignment="Center"
                     ToolTip="Tìm theo Email hoặc Tên"/>
            <Button Content="Tìm kiếm" Width="90" Height="28" Margin="8,0,0,0" Click="BtnSearch_Click"/>
            <ComboBox x:Name="CboStatus" Width="120" Height="28" Margin="16,0,0,0" SelectedIndex="0">
                <ComboBoxItem Content="Tất cả"/>
                <ComboBoxItem Content="Active"/>
                <ComboBoxItem Content="Banned"/>
                <ComboBoxItem Content="Unverified"/>
            </ComboBox>
            <Button Content="Làm mới" Width="80" Height="28" Margin="8,0,0,0" Click="BtnRefresh_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Khóa tài khoản" Width="120" Height="30" Margin="0,0,8,0" Click="BtnBan_Click"/>
            <Button Content="Mở khóa" Width="100" Height="30" Margin="0,0,8,0" Click="BtnUnban_Click"/>
            <Button Content="Chi tiết (Metadata)" Width="130" Height="30" Click="BtnDetail_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="UserGrid" AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="50"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding UserName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="70"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding StatusLabel}" Width="90"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAtStr}" Width="100"/>
                <DataGridTextColumn Header="Đăng nhập cuối" Binding="{Binding LastLoginStr}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
