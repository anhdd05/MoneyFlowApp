<UserControl x:Class="MoneyFlowApp.Admin.AdminCategoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="700">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Grid.RowSpan="2">
            <TabItem Header="Danh mục Giao dịch (Thu/Chi)">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Thêm" Width="80" Height="30" Margin="0,0,8,0" Click="BtnAddTxCat_Click"/>
                        <Button Content="Sửa" Width="80" Height="30" Margin="0,0,8,0" Click="BtnEditTxCat_Click"/>
                        <Button Content="Làm mới" Width="80" Height="30" Click="BtnRefreshTxCat_Click"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" x:Name="TxCategoryGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              SelectionMode="Single" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="50"/>
                            <DataGridTextColumn Header="Tên" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Loại" Binding="{Binding Type}" Width="80"/>
                            <DataGridTextColumn Header="Icon" Binding="{Binding Icon}" Width="80"/>
                            <DataGridTextColumn Header="Màu" Binding="{Binding Color}" Width="80"/>
                            <DataGridTemplateColumn Header="Trạng thái" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ToggleButton Content="{Binding IsActiveLabel}" IsChecked="{Binding IsActive, Mode=OneWay}"
                                                      Click="ToggleTxCat_Click" Tag="{Binding CategoryId}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Danh mục Ngân sách">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Thêm" Width="80" Height="30" Margin="0,0,8,0" Click="BtnAddBudgetCat_Click"/>
                        <Button Content="Sửa" Width="80" Height="30" Margin="0,0,8,0" Click="BtnEditBudgetCat_Click"/>
                        <Button Content="Làm mới" Width="80" Height="30" Click="BtnRefreshBudgetCat_Click"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" x:Name="BudgetCategoryGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              SelectionMode="Single" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="50"/>
                            <DataGridTextColumn Header="Tên" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Loại" Binding="{Binding Type}" Width="80"/>
                            <DataGridTextColumn Header="Icon" Binding="{Binding Icon}" Width="80"/>
                            <DataGridTextColumn Header="Màu" Binding="{Binding Color}" Width="80"/>
                            <DataGridTemplateColumn Header="Trạng thái" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ToggleButton Content="{Binding IsActiveLabel}" IsChecked="{Binding IsActive, Mode=OneWay}"
                                                      Click="ToggleBudgetCat_Click" Tag="{Binding CategoryId}" ToolTip="Bật/Tắt danh mục"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
