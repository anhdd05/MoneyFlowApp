<Window x:Class="MoneyFlowApp.Admin.AdminCategoryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Danh mục" Height="320" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TxtTitle" Text="Thêm danh mục" FontSize="14" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="Tên:" VerticalAlignment="Center" Margin="0,0,0,4"/>
        <TextBox Grid.Row="2" x:Name="TxtName" Height="28" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="3" Text="Loại:" VerticalAlignment="Center" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="4" x:Name="CboType" Height="28" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="5" Text="Icon:" VerticalAlignment="Center" Margin="0,0,0,4"/>
        <TextBox Grid.Row="6" x:Name="TxtIcon" Height="28" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="7" Text="Màu (hex):" VerticalAlignment="Center" Margin="0,0,0,4"/>
        <TextBox Grid.Row="8" x:Name="TxtColor" Height="28" Margin="0,0,0,16" MaxLength="20"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Lưu" Width="80" Height="30" Click="BtnSave_Click" Margin="0,0,8,0"/>
            <Button Content="Hủy" Width="80" Height="30" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
